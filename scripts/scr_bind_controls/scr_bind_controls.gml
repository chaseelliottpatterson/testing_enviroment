//bind_controls()
move_up = keyboard_check(ord("W"));
move_left = keyboard_check(ord("A"));
move_right = keyboard_check(ord("D"));
move_down = keyboard_check(ord("S"));

shoot_up = keyboard_check(vk_up);//or mouse up;
shoot_left = keyboard_check(vk_left)//or mouse left;
shoot_right = keyboard_check(vk_right);//or mouse right;
shoot_down = keyboard_check(vk_down);//or mouse down;